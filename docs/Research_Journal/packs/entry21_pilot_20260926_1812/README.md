# Paper pack `entry21_pilot_20260926_1812`


Everything needed to cite this study in a paper, with provenance.

- **Study:** Safety filter study - PILOT (all prompt positions)
- **Status:** FINAL (45 comparable prompts per arm and mode; 0 run errors)
- **Result file:** `outputs/safety_batches/safety_filter_spec_pilot.json` sha256 `3b36c8f30af7a18589a9ff8123be9ce01c2b75f1466b7e4e083c171d86a2b7b4` (copy in data/result_full.json)
- **Code:** git commit `a9e6302` + uncommitted changes to tracked files - commit before citing

## Files

- `FIGURES.md` - numbered figures with captions and their data source
- `figures/` - each figure as SVG (vector, provenance embedded), PNG (300 dpi) and PDF (vector, for LaTeX)
- `tables/` - every table as CSV and markdown
- `data/` - exact spec, small summary JSON, full result JSON (when small enough)
- `journal_entry_draft.md` - factual draft of the journal entry (interpretation still to write)
- `MANIFEST.json` - machine-readable provenance (settings, versions, checksums)

## Before you cite

1. Status must be **FINAL**.
2. Commit the code (`git status` clean for tracked files) so the commit hash in the manifest is exact.
3. Copy the finished journal entry to `docs/Research_Journal/21.md` and keep this folder next to it.
